{
  "mcpServers": {
    "claudemic": {
      "command": "python3",
      "args": ["-m", "claudemic.server"],
      "cwd": "${CLAUDE_PLUGIN_ROOT}",
      "env": { "PYTHONPATH": "${CLAUDE_PLUGIN_ROOT}/src" }
    }
  }
}
